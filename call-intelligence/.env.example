# Optional: compose-only overrides (WEB_PORT, CORS_ORIGINS, OPENAI_MODEL).
# Your OpenAI key belongs in backend/.env — see backend/.env.example.

# WEB_PORT=8080
# OPENAI_MODEL=gpt-4o-mini
# CORS_ORIGINS=http://localhost:5173,http://localhost:8080,http://127.0.0.1:8080
